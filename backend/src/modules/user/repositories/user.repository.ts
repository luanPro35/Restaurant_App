// Repository pattern implementation if needed, otherwise methods are often on the model/service directly in simple setups.
// But as per requested structure:

export const findByEmail = async (email: string) => {
  // return User.findOne({ email });
};
